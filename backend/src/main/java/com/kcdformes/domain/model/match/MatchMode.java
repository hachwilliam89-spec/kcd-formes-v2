package com.kcdformes.domain.model.match;

/** Mode de jeu multijoueur. Le coop est le premier livré (voir docs/MULTIPLAYER.md). */
public enum MatchMode {
    COOP,        // 2 joueurs défendent la même carte
    VERSUS,      // à venir
    ASYMMETRIC   // à venir
}
